<Page x:Class="CodeLingo.Lesson1.Page10"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CodeLingo.Lesson1"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Page10" Width="448" Height="208">

    <Grid>
        <Label x:Name="Subtitle" Content="displaying text" HorizontalAlignment="Left" VerticalAlignment="Top" FontFamily="Bebas Kai" FontSize="20" Margin="-1,-2,0,0" FontWeight="Bold"/>
        <TextBlock x:Name="Explaination" TextWrapping="Wrap" Text="In our .cpp file in our HelloWorld Project we have our include and using statement at the top of the file. 'cout' allows us to send text to the screen. The 'iostream' library contains the 'namespace std', which has a lot of different names in it, including 'cout'. Instead of 'using namespace std' we could say 'using std::cout' to be specific. For our learning purpose we will just use 'namespace std' to ensure we get all the names we need. So for us to display 'Hello World' out to the screen, we will do the following:" VerticalAlignment="Top" Margin="5,26,9,0"  FontFamily="Bebas Kai" FontSize="16"/>

    </Grid>
</Page>
